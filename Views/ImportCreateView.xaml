<UserControl x:Class="QuanLyBanHang.Views.ImportCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyBanHang.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Tên sản phẩm:" Grid.Row="0" Grid.Column="0" Margin="5"/>
        <TextBox Text="{Binding NewImport.ProductName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Số lượng:" Grid.Row="1" Grid.Column="0" Margin="5"/>
        <TextBox Text="{Binding NewImport.Quantity, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Nhà cung cấp:" Grid.Row="2" Grid.Column="0" Margin="5"/>
        <TextBox Text="{Binding NewImport.Supplier, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Ngày nhập:" Grid.Row="3" Grid.Column="0" Margin="5"/>
        <DatePicker SelectedDate="{Binding NewImport.ImportDate}" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <Button Content="Lưu phiếu nhập" Command="{Binding SaveCommand}" Grid.Row="4" Grid.Column="1" Margin="10" HorizontalAlignment="Right" Width="150"/>
    </Grid>
</UserControl>
