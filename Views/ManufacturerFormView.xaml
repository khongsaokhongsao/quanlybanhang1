<UserControl x:Class="QuanLyBanHang.Views.ManufacturerFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="980" d:DesignWidth="720">
    <UserControl.Resources>
        <!-- Style cho Button KHÔNG bo góc -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="DodgerBlue"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="0" 
                                BorderThickness="1" 
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="RoyalBlue"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="DarkBlue"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Padding="20" CornerRadius="0" Background="White" 
            BorderBrush="Gray" BorderThickness="1">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            

            <!-- Tên nhà sản xuất -->
            <GroupBox Header="Tên nhà sản xuất" Grid.Row="0" Margin="0,5">
                <TextBox Text="{Binding Manufacturer.Name, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            </GroupBox>

            <!-- Địa chỉ -->
            <GroupBox Header="Địa chỉ" Grid.Row="1" Margin="0,5">
                <TextBox Text="{Binding Manufacturer.Address, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            </GroupBox>

            <!-- Số điện thoại -->
            <GroupBox Header="Số điện thoại" Grid.Row="2" Margin="0,5">
                <TextBox Text="{Binding Manufacturer.Phone, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            </GroupBox>

            <!-- Nút lưu và hủy -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                <Button Content="Lưu" Width="100" Height="30" Margin="5" Background="DodgerBlue" Foreground="White"
                    Command="{Binding SaveManufacturerCommand}"/>
                <Button Content="Hủy" Width="100" Height="30" Margin="5" Background="Gray" Foreground="White"
                    Command="{Binding ShowManufacturerListCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
