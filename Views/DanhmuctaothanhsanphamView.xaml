<UserControl x:Class="QuanLyBanHang.Views.DanhmuctaothanhsanphamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyBanHang.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800" Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="DANH MỤC TẠO THÀNH SẢN PHẨM" FontSize="18" FontWeight="Bold" Foreground="Black" Margin="0,0,0,10"/>

        <!-- Nguyên liệu -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,10,0,10">
            <TextBlock Text="🔹 Nguyên liệu / Vật liệu chính" FontSize="16" FontWeight="Bold" Foreground="Black"/>
            <TextBlock Text="Mã nguyên liệu:" FontWeight="SemiBold" Width="96" Height="14" HorizontalAlignment="Left"/>
            <TextBox Width="300" Height="21" RenderTransformOrigin="0.499,0.028"/>
            <TextBlock Text="Tên nguyên liệu:" FontWeight="SemiBold" Width="97" HorizontalAlignment="Left"/>
            <TextBox Width="300"/>
            <TextBlock Text="Số lượng cần dùng:" FontWeight="SemiBold" Width="115" HorizontalAlignment="Left"/>
            <TextBox Width="300"/>
            <TextBlock Text="Giá nhập theo đơn vị:" FontWeight="SemiBold" Width="123" HorizontalAlignment="Left"/>
            <TextBox Width="300"/>
            <TextBlock Text="Nhà cung cấp:" FontWeight="SemiBold" Width="90" HorizontalAlignment="Left"/>
            <TextBox Width="300"/>
        </StackPanel>

        <!-- Nhân công -->
        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,10,0,10">

            <TextBlock Text="🔹 Nhân công / Tiền công sản xuất" FontSize="16" FontWeight="Bold" Foreground="Black"/>

            <TextBlock Text="Thời gian làm việc:" FontWeight="SemiBold"/>
            <TextBox Width="300"/>

            <TextBlock Text="Lương theo giờ/ngày:" FontWeight="SemiBold"/>
            <TextBox Width="300"/>

            <TextBlock Text="Số lượng nhân công tham gia:" FontWeight="SemiBold"/>
            <TextBox Width="300"/>
        </StackPanel>

        <!-- Chi phí mua ngoài -->
        <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,10,0,10">
            <TextBlock Text="🔹 Chi phí mua ngoài" FontSize="16" FontWeight="Bold" Foreground="Black"/>

            <TextBlock Text="Tên dịch vụ / linh kiện:" FontWeight="SemiBold"/>
            <TextBox Width="300"/>

            <TextBlock Text="Nhà cung cấp:" FontWeight="SemiBold"/>
            <TextBox Width="300"/>

            <TextBlock Text="Chi phí:" FontWeight="SemiBold"/>
            <TextBox Width="300"/>
        </StackPanel>

    </Grid>
</UserControl>