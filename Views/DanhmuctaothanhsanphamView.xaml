<UserControl x:Class="QuanLyBanHang.Views.DanhmuctaothanhsanphamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyBanHang.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800" Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Form nhập dữ liệu -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,10">
            <!-- Title -->
            <TextBlock Text="Thêm sản phẩm" FontSize="16" FontWeight="Bold" Foreground="Black" Margin="0,0,0,15"/>

            <!-- Tên Sản Phẩm -->
            <TextBlock Text="Tên Sản Phẩm:" Foreground="Black" FontSize="14" Margin="0,5,0,5"/>
            <TextBox x:Name="txtName" Width="300" Height="30" Foreground="White" Background="#F5F5F5" BorderBrush="#D3D3D3" BorderThickness="1"/>

            <!-- Mô Tả -->
            <TextBlock Text="Mô Tả:" Foreground="Black" FontSize="14" Margin="0,10,0,5"/>
            <TextBox x:Name="txtDescription" Width="300" Height="50" TextWrapping="Wrap"  Background="#F5F5F5" BorderBrush="#D3D3D3" BorderThickness="1"/>

            <!-- Chi Phí Nguyên Liệu -->
            <TextBlock Text="Chi Phí Nguyên Liệu:" Foreground="Black" FontSize="14" Margin="0,10,0,5"/>
            <TextBox x:Name="txtMaterialCost"  Width="300" Height="30" Background="#F5F5F5" BorderBrush="#D3D3D3" BorderThickness="1"/>


            <TextBlock Text="Tiền Công:" Foreground="Black"/>
            <TextBox x:Name="txtLaborCost" Width="300" Height="30" Background="#F5F5F5" BorderBrush="#D3D3D3" BorderThickness="1"/>

            <TextBlock Text="Chi Phí Mua Ngoài:" Foreground="Black"/>
            <TextBox x:Name="txtOutsourceCost" Width="300" Height="30" Background="#F5F5F5" BorderBrush="#D3D3D3" BorderThickness="1"/>

            <TextBlock Text="Lợi Nhuận (%):" Foreground="Black"/>
            <TextBox x:Name="txtProfitMargin" Width="300" Height="30" Background="#F5F5F5" BorderBrush="#D3D3D3" BorderThickness="1"/>

            <!-- Giá Bán -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,5">
                <TextBlock Text="Giá Bán:" Foreground="Black" FontSize="14"/>
                <TextBlock Text=" *" Foreground="Red" FontSize="14"/>
            </StackPanel>
            <TextBox x:Name="txtSellingPrice" Width="300" Height="30" Background="#F5F5F5" BorderBrush="#D3D3D3" BorderThickness="1"/>

            <!-- Tồn kho -->
            <TextBlock Text="Tồn kho:" Foreground="Black" FontSize="14" Margin="0,10,0,5"/>
            <TextBox x:Name="txtStock" Width="300" Height="30" Background="#F5F5F5" BorderBrush="#D3D3D3" BorderThickness="1"/>

            <!-- Trạng thái -->
            <TextBlock Text="Trạng thái:" Foreground="Black" FontSize="14" Margin="0,10,0,5"/>
            <ComboBox x:Name="cmbStatus" Width="300" Height="30" Background="#F5F5F5" BorderBrush="#D3D3D3" BorderThickness="1"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
            </StackPanel>
        </StackPanel>

        <!-- DataGrid hiển thị danh sách sản phẩm -->
        <DataGrid x:Name="dgProducts" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10,0,0" BorderBrush="#D3D3D3" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã SP" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Tên SP" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Giá vốn" Binding="{Binding ProductionCost}" Width="100"/>
                <DataGridTextColumn Header="Lợi Nhuận (%)" Binding="{Binding ProfitMargin}" Width="100"/>
                <DataGridTextColumn Header="Giá Bán" Binding="{Binding SellingPrice}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
