<UserControl x:Class="QuanLyBanHang.Views.DanhmuctaothanhsanphamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuanLyBanHang.Views"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Title -->
            <RowDefinition Height="Auto"/>
            <!-- Nguyên liệu -->
            <RowDefinition Height="Auto"/>
            <!-- Nhân công -->
            <RowDefinition Height="Auto"/>
            <!-- Chi phí mua ngoài -->
            <RowDefinition Height="*"/>
            <!-- Remaining space -->
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="DANH MỤC TẠO THÀNH SẢN PHẨM" FontSize="18" FontWeight="Bold" Foreground="Black" Margin="0,0,0,20"/>

        <!-- Nguyên liệu -->
        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="1" Padding="10" Margin="0,0,0,20">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="🔹 Nguyên liệu / Vật liệu chính" FontSize="16" FontWeight="Bold" Foreground="Black" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Mã nguyên liệu:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="0" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="0" Margin="0,5"/>

                    <TextBlock Text="Tên nguyên liệu:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="1" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="1" Margin="0,5"/>

                    <TextBlock Text="Số lượng cần dùng:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="2" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="2" Margin="0,5"/>

                    <TextBlock Text="Giá nhập theo đơn vị:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="3" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="3" Margin="0,5"/>

                    <TextBlock Text="Nhà cung cấp:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="4" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="4" Margin="0,5"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Nhân công -->
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="1" Padding="10" Margin="0,0,0,20">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="🔹 Nhân công / Tiền công sản xuất" FontSize="16" FontWeight="Bold" Foreground="Black" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Thời gian làm việc:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="0" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="0" Margin="0,5"/>

                    <TextBlock Text="Lương theo giờ/ngày:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="1" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="1" Margin="0,5"/>

                    <TextBlock Text="Số lượng nhân công tham gia:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="2" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="2" Margin="0,5"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Chi phí mua ngoài -->
        <Border Grid.Row="3" BorderBrush="Black" BorderThickness="1" Padding="10" Margin="0,0,0,20">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="🔹 Chi phí mua ngoài" FontSize="16" FontWeight="Bold" Foreground="Black" Margin="0,0,0,10"/>

                <Grid Height="90">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Tên dịch vụ / linh kiện:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="0" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="0" Margin="0,5"/>

                    <TextBlock Text="Nhà cung cấp:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="1" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="1" Margin="0,5"/>

                    <TextBlock Text="Chi phí:" FontWeight="SemiBold" VerticalAlignment="Center" Grid.Column="0" Grid.Row="2" Margin="0,5"/>
                    <TextBox Width="300" Grid.Column="1" Grid.Row="2" Margin="0,5"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
